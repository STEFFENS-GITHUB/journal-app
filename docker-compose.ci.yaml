name: journal
volumes:
  mysql-main:

services:
  mysql:
    image: mysql:latest
    ports:
    - 3306:3306
    volumes: 
    - mysql-main:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  migrate:
    image: steffenp123/journal-api:latest
    entrypoint: alembic -c /api/alembic.ini upgrade head
    environment:
      DATABASE_URL: ${DATABASE_URL}
    depends_on:
      mysql:
        condition: service_healthy

  api:
    image: steffenp123/journal-api:latest
    ports:
    - 8000:8000
    environment:
      DATABASE_URL: ${DATABASE_URL}
      DEFAULT_USER: ${DEFAULT_USER}
      DEFAULT_USER_PASSWORD: ${DEFAULT_USER_PASSWORD}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      SQS_ENDPOINT_URL: http://elasticmq:9324
      EMAIL_VERIFICATION_QUEUE_URL: http://elasticmq:9324/000000000000/email-verification
      REDIS_URL: redis://redis:6379
      AWS_ACCESS_KEY_ID: local
      AWS_SECRET_ACCESS_KEY: local
      AWS_REGION: us-east-1
    depends_on:
      mysql:
        condition: service_healthy
      elasticmq:
        condition: service_healthy
      redis:
        condition: service_healthy

  redis:
    image: redis:latest
    ports:
    - 6379:6379
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s

  elasticmq:
    image: softwaremill/elasticmq-native:latest
    ports:
    - 9324:9324
    - 9325:9325
    volumes:
    - ./elasticmq.conf:/opt/elasticmq.conf
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9324"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s

  worker:
    image: steffenp123/journal-worker:latest
    restart: on-failure:3
    environment:
      SQS_ENDPOINT_URL: http://elasticmq:9324
      EMAIL_VERIFICATION_QUEUE_URL: http://elasticmq:9324/000000000000/email-verification
      VERIFY_API_URL: http://localhost:8000/verify-email
      AWS_ACCESS_KEY_ID: local
      AWS_SECRET_ACCESS_KEY: local
      AWS_REGION: us-east-1
    depends_on:
      elasticmq:
        condition: service_healthy